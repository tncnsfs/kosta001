<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "kogile.checklist.Mapper.CheckListMapper">
	<cache/>
	<insert id="insertchecklist" parameterType = "Board"> 
 		insert into checklist values(checklist_seq.nextval+5, #{check_title},(2))
	</insert>
	

<!-- 	<insert id="insertchecklist" parameterType = "Board"> 
 	insert into checklist values(#{checklist_no}, #{check_title},
 							#{p_no})
	</insert>
	
 -->	
	
<!-- 	<insert id="insertchecklist1"> 
		insert into checklist values(19,'title102',2)		
	</insert>
 -->	
	
</mapper>